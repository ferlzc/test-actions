name: PUSH master
on:
  push:
    paths:
      - '**'
      - '.github/configs/**'
jobs:

  build-raspberrypi-armv8-scarthgap:
    uses: ./.github/workflows/raspberrypi-armv8-scarthgap.yaml
    with:
      # meta-pantavisor
      configs: .github/configs/shared-vols.yaml:.github/configs/release/buildkas.yaml
      name: raspberrypi-armv8
