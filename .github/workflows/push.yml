name: PUSH master
on:
  push:
    paths:
      - '**'
      - '.github/**'
      - '.github/configs/**'
jobs:

  call-deploy-rasp:
    name: call 1
    uses: .github/workflows/raspberrypi-armv8-scarthgap.yaml

  call-deploy-rasp2:
    name: call 2
    uses: .github/workflows/raspberrypi-armv8-scarthgap.yml